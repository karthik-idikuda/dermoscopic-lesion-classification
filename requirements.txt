# Runtime dependencies. Versions are lower-bounded rather than pinned so the
# project installs cleanly against a CUDA-matched torch wheel; pin exactly for a
# reproducible deployment.
#
#   pip install -r requirements.txt
#
# For GPU training, install torch from the index that matches your CUDA version
# first, then run the line above:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# --- deep learning ---
torch>=2.1
torchvision>=0.16
timm>=0.9.12

# --- classical CV and ML ---
numpy>=1.24,<3
pillow>=10.0
opencv-python-headless>=4.8
scikit-image>=0.21
scikit-learn>=1.3
pandas>=2.0
joblib>=1.3

# --- plotting (training/evaluation artefacts) ---
matplotlib>=3.7
seaborn>=0.12

# --- web service ---
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.5

# --- reporting ---
reportlab>=4.0
